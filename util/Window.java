package util;

 	// in case of resizing the window we would not need to find all instances of the windows 
	// resolution being used
public class Window {
	public static final int WIDTH = 800;
	public static final int HEIGHT = 600;
}
